# jepa-ctrl dependencies. Python 3.12, laptop RTX 5080 (Blackwell sm_120).
#
# Install torch FIRST from the cu128 index (Blackwell needs CUDA 12.8 wheels):
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# then:  pip install -r requirements.txt
torch>=2.11            # via cu128 index, see note above
mujoco==3.8.1          # PINNED: dm_control 1.0.41 breaks on mujoco 3.9.0 (MjModel.flex_bandwidth removed)
dm_control==1.0.41
gymnasium==1.3.0
imageio==2.37.3
imageio-ffmpeg==0.6.0
matplotlib>=3.10
numpy>=2.0
scipy>=1.13
tqdm
pyyaml
pytest
ruff
